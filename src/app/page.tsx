import { DashboardPageClient } from '@/components/dashboard-page-client';

export default function Home() {
  // This Next.js application provides a frontend mock-up inspired by a professional dashboard.
  // It uses sample client-side data for demonstration purposes.
  // Apache Superset specific features like SQLAlchemy models or Superset's i18n/CI-CD are conceptualized here
  // through Next.js equivalents (TypeScript types, client-side data, React context for i18n).
  return <DashboardPageClient />;
}
